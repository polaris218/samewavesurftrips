import React from 'react';
import { Container, P } from 'components';
import { Spacings } from 'config';

const SearchScreen = props => {
  return (
      <Container>
        <P fontSize={Spacings.FONT.HEADER}>Search Screen</P>
      </Container>
  );
}

export default SearchScreen;
